server:
  port: 7008
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/forezp/SpringcloudConfig/
          search-paths: respo
      label: master
      profile: dev
eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka/